/**
 * Hook that grabs recruitment entries (applications) from the backend and sanitizes the data.
 * Can also be used to modify entries using application profile.
 */
const useRecruitmentEntries = () => {};
